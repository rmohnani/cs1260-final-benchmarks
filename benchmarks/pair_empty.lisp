(define (make-pair x y) (pair x y))
(let ((x (make-pair 1 2)))
    (let ((y (make-pair 10 x)))
        (let ((z ()))
            (do (print (pair? x))
                (print (pair? y))
                (print (pair? z))
                (newline)
                (print (empty? x))
                (print (empty? y))
                (print (empty? z))))))